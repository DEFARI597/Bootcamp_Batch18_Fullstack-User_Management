<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Users UMS</h1>
    <ul>
        <% data.forEach(user=> { %>
            <li>Nama: <%= user.name || '-' %> | Email: <%= user.email || '-' %> | No. HP: <%= user.phone || '-' %>
            </li>
            <% }) %>
    </ul>
</body>

</html>